import React from 'react';
import { render, screen } from '@testing-library/react';
import ScanPage from './ScanPage';

describe('ScanPage tests', () => {
  it('should contains the heading 1', () => {
    expect(1).toBe(1);
  });
});